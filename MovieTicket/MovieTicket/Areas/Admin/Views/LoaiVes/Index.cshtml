@model IEnumerable<MovieTicket.Models.LoaiVe>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink(" Thêm loại vé", "Create", "", htmlAttributes: new { @class = "fa fa-plus-circle create" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tenloaive)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.giave)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tenloaive)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.giave)
        </td>
        <td>
            @Html.ActionLink(" ", "Edit", new { id = item.maloaive }, new { @class = "fa fa-edit option", @title = "Chỉnh sửa" }) |
            @Html.ActionLink(" ", "Details", new { id = item.maloaive}, new { @class = "fa fa-info-circle option", @title = "Chi tiết" }) @*|
            @Html.ActionLink("Delete", "Delete", new { id=item.maloaive })*@
        </td>
    </tr>
}

</table>
